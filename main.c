#include "push_swap.h"
#include <stdio.h>

int	main(int args, char **argv)
{
	t_stack	*a;
	t_stack	*b;

	if (args == 1 || (args > 2 && !argv[1][0]))
	{
		printf("Error\n");
		return (0);
	}
	argv = ft_split(argv);
	return (0);
}
